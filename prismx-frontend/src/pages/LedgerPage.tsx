import PageWrapper from '../components/layout/PageWrapper';
export default function LedgerPage() {
  return <PageWrapper title="Ledger"><div style={{color:'var(--text-secondary)'}}>Ledger — coming soon</div></PageWrapper>;
}